# 2026-06-25 Work Log

## BuddyLink Build Completion
- Completed `cargo tauri build` for BuddyLink macOS app
- Build time: 8m 36s (release profile, 516 crates)
- Output: BuddyLink.app (12MB, Mach-O 64-bit arm64) at `client/src-tauri/target/release/bundle/macos/BuddyLink.app`
- dmg packaging failed due to sandbox restrictions on hdiutil - can be created manually
- Tauri CLI v2.11.3 installed successfully
- All 7 tasks completed (plan → server → Tauri skeleton → crypto/pairing → UI/icons → build)
- Fixed identifier warning: changed `com.buddylink.app` → `com.buddylink.desktop`

## Bug Fixes & dmg Creation
- 修复3个关键运行问题：(1) transparent+no-decorations导致窗口隐形 → 改为decorations:true+transparent:false;(2) ES模块onclick函数不暴露到全局 → 添加16个window.xxx导出;(3) codesign签名缺失 → ad-hoc签名
- 成功创建可分发dmg: BuddyLink_1.0.0_aarch64.dmg (12MB), 包含BuddyLink.app + Applications快捷方式
- dmg创建方式: `hdiutil makehybrid -hfs` (标准create方式在沙箱中被macOS阻止)
- 中继服务器运行: `cd server && NODE_PATH=... node src/index.js` 后台端口8443

## 2026-06-26 更新
- 修复闪退问题: db_path从相对路径改为Tauri app_data_dir (~Library/Application Support/com.buddylink.desktop/)
- lib.rs重构: 所有.expect()改为match+日志错误处理, 加RUST_BACKTRACE+env_logger
- 重新构建 .app (June 26 10:17) 并重新生成 dmg (June 26 10:22, 包含所有修复)
- 注意: 默认服务器地址已改为 wss://buddylink-relay.onrender.com，公网可达
- 服务器代码改造: 添加Express HTTP健康检查(/health)、CORS支持、环境变量PORT配置、render.yaml蓝图
- 新增express依赖到server/package.json
- Render.com部署: 用户在 https://dashboard.render.com 创建Web Service，连接GitHub repo buddylink-relay
- 2026-06-26 11:19: 重新构建 .app 和 dmg，默认服务器地址 wss://buddylink-relay.onrender.com
