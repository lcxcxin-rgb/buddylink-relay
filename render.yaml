services:
  - type: web
    name: buddylink-relay
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: PORT
        fromService:
          type: web
          name: buddylink-relay
          property: port
    healthCheckPath: /health
