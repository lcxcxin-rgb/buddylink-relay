services:
  - type: web
    name: buddylink-relay
    runtime: node
    plan: free
    buildCommand: cd server && npm install
    startCommand: cd server && node src/index.js
    envVars:
      - key: PORT
        fromService:
          type: web
          name: buddylink-relay
          property: port
    healthCheckPath: /health
